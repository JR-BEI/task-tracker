Read .claude/test-config.json for project settings.
Use Playwright MCP to test data persistence:
1. Create item at {{baseUrl}}
2. Note the item ID from URL
3. Refresh and verify item exists
4. Navigate away and back
5. Verify data intact
6. Screenshot final state
